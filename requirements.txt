# Core scraper dependencies
beautifulsoup4>=4.12.0
curl-cffi>=0.5.0
tldextract>=3.4.0
urllib3>=2.0.0
aiohttp>=3.8.0
pypdf>=3.17.0
python-dotenv>=1.0.0
duckduckgo-search>=3.9.0
httpx>=0.24.0

# Scraper basic UI (--ui mode)
flask>=2.3.2

# Data processing
pandas>=2.0.0

# Security - HTML sanitization
bleach>=6.0.0
tinycss2>=1.2.0

# Optional: PDF export
reportlab>=4.0.0

# Dashboard enhancements
psutil>=5.9.0
openpyxl>=3.1.0

# Login handler (optional)
selenium>=4.0.0

# Testing and Code Quality
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-asyncio>=0.21.0
pytest-mock>=3.11.0
coverage>=7.3.0

# Code Quality Tools
flake8>=6.1.0
black>=23.7.0
isort>=5.12.0
pylint>=3.0.0
mypy>=1.5.0

# Note: Chart.js is loaded via CDN in the HTML templates
# Note: Tailwind CSS is loaded via CDN in the HTML templates
# Note: flask-cors removed - only used by deprecated dashboard, not needed for basic scraper UI
