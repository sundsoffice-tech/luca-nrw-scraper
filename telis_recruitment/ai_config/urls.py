"""
AI Config URLs

Minimal URL configuration for namespace readiness.
Currently, all functionality is admin-only.
"""

from django.urls import path

app_name = 'ai_config'

urlpatterns = [
    # No public endpoints needed - all functionality is in admin
    # This file exists for namespace readiness
]
