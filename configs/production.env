# ==========================
# PRODUCTION SAFE PROFILE
# ==========================
# 
# ⚠️  SECURITY WARNING ⚠️
# This configuration file contains EXAMPLE VALUES ONLY!
# DO NOT use these values in production without customization.
# 
# REQUIRED ACTIONS BEFORE DEPLOYMENT:
# 1. Generate a unique SECRET_KEY (see below)
# 2. Set your actual production domain(s) in ALLOWED_HOSTS
# 3. Configure all API keys with real values
# 4. Update database credentials if using PostgreSQL
# 5. Never commit this file with real secrets to version control
#
# The application will ABORT startup if critical values are not set.
#
# Security Level: HIGH
# Performance: BALANCED
# Logging: MINIMAL
# 
# ==========================

# ==========================
# Django Core Settings
# ==========================

# REQUIRED: Generate a unique secret key for production
# Generate with: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
# ⚠️  CRITICAL: This MUST be changed before deployment!
SECRET_KEY=CHANGE-ME-GENERATE-UNIQUE-SECRET-KEY-FOR-PRODUCTION

# CRITICAL: Never enable DEBUG in production!
DEBUG=False

# REQUIRED: Set your production domains
# Example: ALLOWED_HOSTS=example.com,www.example.com
# ⚠️  CRITICAL: This MUST be set to your actual domain(s)!
ALLOWED_HOSTS=your-domain.com,www.your-domain.com

# Set your production URLs with HTTPS
# Example: CSRF_TRUSTED_ORIGINS=https://example.com,https://www.example.com
CSRF_TRUSTED_ORIGINS=https://your-domain.com,https://www.your-domain.com

# ==========================
# Database Settings
# ==========================

# For production, consider PostgreSQL instead of SQLite
DATABASE_URL=sqlite:///db.sqlite3
# PostgreSQL example:
# DATABASE_URL=postgresql://user:password@localhost:5432/luca_db

# ==========================
# Static & Media Files
# ==========================

STATIC_ROOT=staticfiles
MEDIA_ROOT=media

# ==========================
# Scraper Configuration (Conservative)
# ==========================

SCRAPER_PATH=../
SCRAPER_MODE=once
SCRAPER_QPI=6
SCRAPER_DATE_RESTRICT=d30
SCRAPER_DEFAULT_INDUSTRY=recruiter

# ==========================
# Security Settings (PRODUCTION)
# ==========================

# SSL/TLS: ALWAYS validate certificates in production
ALLOW_INSECURE_SSL=0

# Conservative timeouts and limits
HTTP_TIMEOUT=15
MAX_FETCH_SIZE=2097152
ASYNC_LIMIT=25
ASYNC_PER_HOST=2
HTTP2=1

# Proxy (if required by your infrastructure)
HTTP_PROXY=
HTTPS_PROXY=
SOCKS_PROXY=

# ==========================
# Django Security (HTTPS Required)
# ==========================

# Enable these ONLY if you have SSL/HTTPS configured
SECURE_SSL_REDIRECT=True
SESSION_COOKIE_SECURE=True
CSRF_COOKIE_SECURE=True
SECURE_HSTS_SECONDS=31536000

# ==========================
# API Keys
# ==========================

# ⚠️  IMPORTANT: Replace empty values with real API keys
# These are REQUIRED for the application to function properly.
# Use environment-specific keys, never commit real keys to git.
# 
# Recommended: Use secret management systems like:
# - Docker Secrets
# - HashiCorp Vault
# - AWS Secrets Manager
# - Azure Key Vault

# OpenAI API Key (for AI features)
OPENAI_API_KEY=

# Perplexity API Key (for AI search)
PERPLEXITY_API_KEY=

# Brevo API Key (REQUIRED for email marketing features)
# ⚠️  Application will show warnings if not set
BREVO_API_KEY=

# Google Custom Search (for web scraping)
GOOGLE_API_KEY=
GOOGLE_CSE_ID=

# TinyMCE Rich-Text-Editor (kostenlos auf https://www.tiny.cloud/)
# Get your free API key at: https://www.tiny.cloud/
# Leave as 'no-api-key' for development (shows warning but works)
TINYMCE_API_KEY=no-api-key

# ==========================
# Logging
# ==========================

# Production: INFO level to reduce noise
LOG_LEVEL=INFO

# ==========================
# Brevo Email Marketing
# ==========================

BREVO_DEFAULT_LIST_ID=
BREVO_LANDING_PAGE_LIST_ID=
BREVO_WELCOME_TEMPLATE_ID=
BREVO_WEBHOOK_SECRET=
